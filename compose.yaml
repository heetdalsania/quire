services:
  quire:
    build: .
    # Bound to loopback deliberately. Quire has no authentication: anyone who can reach
    # this port can read and edit every document. See SECURITY.md before widening it.
    ports:
      - "127.0.0.1:4321:4321"
    volumes:
      - ./vault:/vault
    restart: unless-stopped
