services:
  quire:
    build: .
    # Bound to loopback deliberately. External access needs a scoped share capability and
    # a trusted VPN or TLS terminator; Quire itself does not provide TLS.
    ports:
      - "127.0.0.1:4321:4321"
    volumes:
      - ./vault:/vault
    restart: unless-stopped
