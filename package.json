{
  "name": "quire-monorepo",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc --build && npm run build -w @quire/web",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --build --pretty",
    "start": "node packages/cli/bin/quire.js"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}